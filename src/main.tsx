import React from "react";
import ReactDOM from "react-dom/client";
import WaveOpticsSimulation from "./WaveOpticsSimulation";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <WaveOpticsSimulation />
);
